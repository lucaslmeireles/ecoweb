export type articleType = {
         uri: string,
         url: string,
         image : string,
         title : string,
         body : string,
         source : {
            uri: string
         }
}
